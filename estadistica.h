//funcion de calculo
void calculos(int year, datos GWh[], int numYear);

void compilar_resultados(int numYear, resultado estadistica[]);

void imprimir_resultados(int numYeat, resultado estadistica[]);